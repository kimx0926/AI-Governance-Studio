# Copy to .env and fill in your key to enable live LLM calls.
# Without a key, policy_engine.py falls back to a clearly-labeled mock
# response so the rest of the pipeline still runs end-to-end.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-5-20250929
